<h1>ユーザー詳細</h1>

<hr>
<P><span>name: </span><%= @user.name %></P>
<p><span>email: </span><%= @user.email %></p>
<P><span>profile: </span><%= @user.profile %></P>
<hr>

<% @flights.each do |flight| %>
  <p><span>投稿内容:</span><%= link_to flight.departure_airport, flight_path(flight.id) %></p>
<% end %>